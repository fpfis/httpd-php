workspace:
  base: ${DRONE_BRANCH%%/*}
  path: ${DRONE_BRANCH##*/}

pipeline:
  build-and-push-image:
    image: plugins/docker
    repo: fpfis/httpd-php
    tags: [ 7.1 ]
#	- ${DRONE_BRANCH:8:1}
#        - ${DRONE_BRANCH##*/}
    secrets: [ docker_username, docker_password ]
    when:
      branch: release/*
