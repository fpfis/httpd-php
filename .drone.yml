workspace:
  base: /fpfis/
  path: httpd-php

pipeline:
  dumpenv:
    image: library/bash
    commands:
      - echo ${DRONE_BRANCH:9}
      - echo ${DRONE_BRANCH##*/}
#  build-and-push-image:
#    image: plugins/docker
#    repo: fpfis/httpd-php
#    tags: [ 5 , 5.6 , latest ]
#    secrets: [ docker_username, docker_password ]
#    when:
#      branch: release/*
